<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">

    <!-- Always force latest IE rendering engine or request Chrome Frame -->
    <meta content="IE=edge,chrome=1" http-equiv="X-UA-Compatible">

    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="keywords" content="vancouver east village, east village vancouver, vancouver east village directory, vancouver east village store locator, vancouver east village shops, east village vancouver shops, vancouver east village restaurants, east village vancouver restaurants, vancouver east village services, east village vancouver services">
    <meta name="description" content="The Vancouver East Village Store Directory">

    <!-- Use title if it's in the page YAML frontmatter -->
    <title><%= current_page.data.title || "The Middleman" %></title>
    <%= stylesheet_link_tag "all" %>

  </head>

  <body class="<%= page_classes %>">

    <button type="button" class="js-menu-trigger sliding-menu-button">
      <img src="images/global/menu-white.png" alt="Show Category Filters">
    </button>

    <nav class="js-menu sliding-menu-content">
      <h2>Categories</h2>
      <ul class="categories">
        <% data.directory.navigation.categories.each do |category| %>
          <li><a href="<%= category.name %>" data="<%= category.name.downcase %>"><%= category.name %></a>
            <% unless category.subcategories.nil? %>
              <ul class="subcategories">
                <% category.subcategories.each do |subcategory| %>
                  <li><a href="<%= subcategory.name %>"><%= subcategory.name %></a></li>
                <% end %>
              </ul>
            <% end %>
          </li>
        <% end %>
      </ul>
    </nav>

    <div class="js-menu-screen menu-screen"></div>

    <%= yield %>

    <footer>
      Created by <a href="http://www.funnelcity.com"> funnelcity</a> &copy; <%= Date.today.strftime("%Y") %>
    </footer>

    <div id="main" role="main"></div>

    <%= javascript_include_tag  "map" %>
    <%= javascript_include_tag  "all" %>

  </body>
</html>